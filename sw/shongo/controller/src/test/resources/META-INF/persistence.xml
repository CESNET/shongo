<?xml version="1.0" encoding="UTF-8"?>
<persistence version="1.0"
             xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd">

    <persistence-unit name="test">

        <provider>org.hibernate.ejb.HibernatePersistence</provider>

        <!-- Map all entities from source to Hibernate -->
        <jar-file>${project.build.outputDirectory}</jar-file>

        <!-- Hibernate configuration -->
        <properties>
            <property name="hibernate.connection.url" value="jdbc:hsqldb:mem:controller; shutdown=true"/>
            <property name="hibernate.connection.driver_class" value="org.hsqldb.jdbcDriver"/>
            <property name="hibernate.connection.username" value="sa"/>
            <property name="hibernate.connection.password" value=""/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.HSQLDialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.ejb.naming_strategy" value="cz.cesnet.shongo.controller.util.NamingStrategy"/>
        </properties>

    </persistence-unit>

</persistence>
