<?xml version="1.0" encoding="UTF-8" ?>

<!-- Default Configuration of Domain Controller -->
<configuration>

    <!-- Specifies whether XML-RPC requests and responses should be logged -->
    <log-rpc>false</log-rpc>

    <!-- Configuration of controlled domain -->
    <domain>
        <name>unknown</name>
        <organization>Unknown Organization</organization>
    </domain>

    <!-- Database settings -->
    <database>
        <filename>data/controller</filename>
    </database>

    <!-- Configuration of XML-RPC server -->
    <rpc>
        <host></host>
        <port>8181</port>
    </rpc>

    <!-- Configuration of Jade container -->
    <jade>
        <host>127.0.0.1</host>
        <port>8282</port>
        <platform-id>Shongo</platform-id>
    </jade>

    <!-- Security settings -->
    <security>
        <!-- Authorization server -->
        <authorization-server>https://hroch.cesnet.cz/phpid-server/oic/</authorization-server>
        <!-- Specifies access token which won't be verified and can be used for testing purposes -->
        <testing-access-token>1e3f174ceaa8e515721b989b19f71727060d0839</testing-access-token>
    </security>

    <!-- Worker periodically starts preprocessor and scheduler -->
    <worker>
        <!-- Period in which the worker is periodically working -->
        <period>PT10S</period>
        <!-- Length of working interval -->
        <interval>P31D</interval>
    </worker>

    <!-- Executor periodically checks compartments for execution -->
    <executor>
        <!-- Period in which the executor is periodically checking compartments -->
        <period>PT15S</period>
        <!-- Configuration of lookup ahead for compartments to be executed -->
        <lookup-ahead>PT3M</lookup-ahead>
        <!-- Configuration for each executable -->
        <executable>
            <!-- Period modifying compartment starting date/time -->
            <start>PT-30S</start>
            <!-- Period modifying compartment ending date/time -->
            <end>PT-30S</end>
            <!-- Period for checking starting date/time -->
            <waiting-start>PT10S</waiting-start>
            <!-- Period for checking ending date/time -->
            <waiting-end>PT30S</waiting-end>
        </executable>
        <!-- Configuration for compartment executable -->
        <compartment>
            <!-- Duration of waiting for virtual rooms to be created -->
            <waiting-room>PT5S</waiting-room>
        </compartment>
    </executor>

    <!-- Reservation configuration -->
    <reservation>
        <!-- Configuration for reservation of resources -->
        <resource>
            <!-- Maximum duration of a single resource reservation -->
            <max-duration>P1W</max-duration>
        </resource>

        <!-- Configuration for reservation of aliases -->
        <alias>
            <!-- Maximum duration of a single alias reservation -->
            <max-duration>P1Y</max-duration>
        </alias>
    </reservation>

    <!-- Configuration for sending emails -->
    <smtp>
        <port>25</port>
    </smtp>

</configuration>
