;(function($){
    $.fn.timepicker.showSeconds = true;
    $.fn.timepicker.defaults.showSeconds = true;
    $.fn.timepicker.showMeridian = false;
    $.fn.timepicker.defaults.showMeridian = false;
}(jQuery));
